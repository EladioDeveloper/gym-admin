import * as React from "react";
import { Card, CardContent, CardHeader } from "@material-ui/core";

export default () => (
  <Card>
    <CardHeader title="Bienvenidos al Dashboard del Gimnasio JERC" />
    <CardContent>Por favor, seleccione algun menu de la izquierda.</CardContent>
  </Card>
);
